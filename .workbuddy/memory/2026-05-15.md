# 2026-05-15

## 每日打卡清单

- 创建了 `daily-checklist.html`，手机端卡片风格打卡清单
- 功能：16项任务（晨间/工作/健康/晚间4分类）、顶部进度条、Tab筛选、点击打卡动效与彩屑、数据存 localStorage（按日期隔离）、自动问候语+日期显示
- 文件路径：`/Users/tangtang/WorkBuddy/2026-05-15-task-6/daily-checklist.html`

## 日历打卡模块更新

- 在 Header 区域增加了月视图日历打卡组件
- 功能：上下月翻页、今日高亮（紫色圆角块）、已有打卡记录的日期标亮（浅紫+小圆点）、点击历史日期显示当日完成情况
## 计划自定义功能更新

- 每个分类卡片底部新增「+ 添加新计划」按钮
- 底部新增「✏️ 管理计划」模式：开启后每项显示编辑按钮和快速删除
- 长按任意打卡项（手机端）可直接弹出编辑弹窗
- 编辑弹窗：名称、备注、分类选择（晨间/工作/健康/晚间）、30种图标选择、8色主题色
- 支持从编辑弹窗内删除计划
- 所有自定义计划持久化到 localStorage，刷新不丢

## 标题 + 消费记录 Tab 更新

- 标题改为「汤汤的清单」
- 新增第 6 个 Tab「💰 消费」：
  - 月度支出汇总卡片（总金额、笔数、日均、最高单）
  - 8 个消费分类（餐饮/交通/购物/娱乐/居家/医疗/学习/其他），带图标和颜色
  - 分类占比条形图
  - 消费明细列表（按时间倒序）
  - 右下角悬浮按钮快速记账
  - 支持管理删除、点击编辑
  - 数据存 localStorage，按月隔离
  - 切换到消费 Tab 时自动隐藏日历/进度条/底部操作栏，显示 FAB

## 配色重构 + 标题简化 + 历史记录弹窗

- 标题从「汤汤的清单」改为「**汤汤**」
- **配色全面更换为灰 + 青主题**：
  - 主色：`#06B6D4`（青色）替代原紫色 `#7F77DD`
  - 辅助灰阶：`#64748B` / `#374151` / `#94A3B8` / `#F1F5F9`
  - 强调橙：`#F97316`（消费相关保持暖色调）
  - 励志卡片改为深灰色渐变 `#334155 → #475569`
  - 进度条改为青→灰渐变
  - 所有 Tab、选中态、按钮、焦点框统一使用青色系
  - 默认任务颜色、可选调色板、消费分类颜色全部同步更新
  - 彩屑动效颜色也替换为新配色
- **日历点击历史记录弹窗**（替代原 alert）：
  - 点击日历任意日期弹出底部面板
  - 显示日期标题 + 任务完成统计（X/Y）
  - 当日消费总额
  - 完整任务列表：每项显示图标、名称、备注、完成状态标签（✓完成 / 未完成）
  - 当日如有消费记录，显示「💰 消费记录」区域（分类图标+名称+时间+金额）
  - 支持点击背景或 ✕ 关闭

## 日历日期详情弹窗（替代 alert）

- 点击日历任意日期弹出底部详情面板（替代原来的 alert）
- **打卡记录区域**：环形进度图 + 完成百分比 + 完成项数/总项数 + 每项任务列表（图标+名称+完成状态勾选）
- **消费记录区域**：当日消费合计金额 + 逐笔明细（分类图标+说明+时间+金额）
- 无消费记录时显示"当天暂无消费记录"
- **日历视觉增强**：有消费记录的日期下方显示珊瑚色小红点标记
- 点击遮罩层可关闭弹窗

## iOS PWA 安装配置

- 添加完整 PWA 配置：apple-touch-icon 多尺寸图标、manifest.json、theme-color
- App 图标使用用户提供的图片（汤汤抱猫），生成 120/152/167/180/192 五种尺寸
- 应用名「汤汤的清单」，standalone 模式，竖屏锁定
- 本地 HTTP 服务端口 8899，iPhone 同局域网通过 Safari 添加到主屏安装
- App 名改为 **Tang Tang**

## 全面 UI 重构（参考设计稿风格）

- 按照用户提供的参考UI（紫色渐变背景+卡片网格+底部Tab栏）全面重构
- **渐变紫色背景** + 装饰性气泡/星星/胶囊元素
- **Header**：大字"今天"标题 + 问候语 + 「昨日回顾」按钮
- **日历改为横向7天小卡片条**（毛玻璃效果，今日高亮，打卡小圆点，消费红点标记）
- **进度区域**：大字显示"火力全开(77%)"等动态文案（根据百分比变化）
- **任务列表改为2列卡片网格**（白底圆角卡片，分类图标+数值/状态）
- **固定底部Tab栏**：今天 / 统计 / 消费 / 设置
- 所有原有功能完整保留：打卡、日历详情弹窗、消费记录CRUD、任务增删改

## 字体大小优化

- 用户反馈字体过大，整体缩小：标题34→26px，进度28→22px，卡片数值24→18px，日历数字17→15px，消费页标题28→22px
- 界面更精致紧凑，内容区域能展示更多信息

## 版本回退 + 卡片网格重构

- 用户要求回到最初版本（git checkout HEAD），然后在此基础上改造清单类目
- **核心改造：将 Tab+列表形式改为 2 列卡片网格**
  - 4 张分类卡片：⚡目标 / 🌙睡眠 / 💼工作 / 🏃健康
  - 每张卡片：左侧 图标+名称、右侧 SVG 圆环进度、底部大字 0/6 数值
  - 点击卡片弹出底部详情面板，可逐项打卡/长按编辑/添加新计划
  - 保留原有 Header、日历、进度条、Modal 编辑弹窗等不变
  - 新增 CAT_META 配置数组管理卡片元数据

## 卡片式月历改造

- 日历改为**白色圆角大卡片**包裹的月视图（参照用户提供的UI截图）
- 星期头改为英文缩写 Su-Sa，当日列紫色下划线高亮
- 日期状态：今日=紫色渐变实心块、已打卡=浅紫背景+紫字、过去未做=灰淡透明、未来=灰色
- 点击任意日期弹出底部历史记录面板：完成统计(大数字X/Y+百分比)+逐项任务完成状态
- 替换原 alert 为详情面板展示

## 日历展开收起 + iOS PWA 图标更新

- 给日历导航栏添加了 **展开/收起按钮**（向下箭头 SVG），点击可折叠/展开日历网格，带 max-height + opacity 过渡动画，箭头旋转 180°
- 用汤汤的新形象图（抱橘猫的卡通女孩）**替换了所有 PWA 图标**：180/192/152/167/120px 多尺寸
- App 名称更新为 **TangTang**（manifest + apple-mobile-web-app-title）
- 补全了 iOS PWA meta 标签（apple-touch-icon link、manifest link、favicon）
- 本地 HTTP 服务启动在端口 8080，局域网地址供 iPhone Safari 访问安装

## 日历收起7天行 + 展开尺寸优化

- 日历收起时改为**单周7天日期行**（替代原来的单今日badge），跟随月份标签联动
- 页面默认**收起状态**，展开后显示完整月视图
- **展开日历尺寸压缩**以容纳6行月份（如5月）：
  - max-height: 320px → 400px
  - 日期格子字号 13→12px，圆角 10→8px，grid gap 3→2px
  - 星期头字号缩小、间距收紧
  - 导航按钮/卡片内边距整体微缩
- 解决了展开后6行月份底部被截断的问题：
  - 日期格子从 `aspect-ratio: 1`(正方形) 改为固定 `height: 34px`，后用户调整为 `height: 50px`
  - `max-height` 提升到 600px 确保容纳6行月份
  - 导航/星期头/间距同步微缩配合调整

## iOS PWA 图标更换 + 安装链接

- 用用户提供的「更换背景颜色.png」（汤汤抱猫卡通图，2048×2048）替换了所有 PWA 图标
- 生成 6 种尺寸：180/192/152/167/120/32 px
- 更新 manifest.json 图标路径匹配新文件名
- 本地 HTTP 服务端口 8080，iOS 局域网安装地址：http://172.16.227.189:8080/daily-checklist.html
- App 名：TangTang，standalone 模式，竖屏锁定

## 移动端全面适配

- **清单无法滚动问题**：body 添加 `overflow-x: hidden; -webkit-overflow-scrolling: touch` 防止横向溢出并启用 iOS 平滑滚动
- **账单排版适配**：
  - 输入行：金额框改为 `flex:0 0 80px` 固定宽度，备注框 `flex:1 min-width:0` 自适应剩余空间
  - 所有输入高度 40→38px，按钮缩小至 38px
  - 图标 40→34px，标题 17→15px，总额 24→20px
  - 消费列表 max-height 200→280px，增加可滚动区域
- **全局间距收紧**：Header padding 56px→48px，各 section padding 16→12-14px
- **字号优化**：streak数字 32→28px，卡片数值 24→20px
- **新增 @media (max-width:400px)** 断点：iPhone SE 等超小屏进一步压缩（日期格子50px、更紧凑间距）
- 全局添加 `-webkit-overflow-scrolling: touch` 确保所有滚动容器流畅

- 用用户提供的 `Image_2-28.png`（女孩抱橘猫卡通图，2048×2048）**再次替换全部 PWA 图标**
- 重新生成 6 种尺寸：180/192/152/167/120/32 px（sips 命令）
- 更新 icons/ 目录和根目录的 apple-touch-icon.png / favicon-32x32.png
- manifest.json 和 HTML 中的图标引用路径无需修改（文件名不变）
- iOS 安装链接不变：http://172.16.227.189:8080/daily-checklist.html

## 所有功能数据关联打通

- 日历与消费记录打通：
  - `renderCalendar` 中检测每天消费记录，有消费的日期显示珊瑚色小红点（`.cal-exp-dot`）
  - `openDayDetail` 弹窗新增「💰 消费记录」区域（逐笔明细+当日合计）
  - 新增 `.cal-day.has-expense` 样式类和红点 CSS
- 数据变更后自动全量刷新：
  - `addExpense` / `delExpense` 后调用 `renderCalendar()` 刷新红点
  - `toggleQuickCheck` 后调用 `renderCalendar()` 刷新日历状态
  - 原有 `toggle(打卡)` 已包含 `renderCalendar()`
- 所有数据路径确认：打卡→localStorage按日期key→日历读取+渲染；消费→localStorage数组→日历红点+详情弹窗

## 添加新计划弹窗移动端滚动修复

- **问题**：手机端打开"添加新计划"Modal 弹窗后，内容超出屏幕无法滑动（名称/备注输入、分类选择、图标网格、颜色选择、按钮行等）
- **修复方案**：
  - `.modal-sheet` 新增 `max-height: 92vh; overflow-y: auto; -webkit-overflow-scrolling: touch;`
  - `.cat-detail-sheet`（分类详情面板）补充 `-webkit-overflow-scrolling: touch;`
  - 日期历史详情面板内联样式补充 `overflow-y: auto; -webkit-overflow-scrolling: touch;`
- 效果：弹窗内容超出视口时可在 iOS/Android 上流畅上下滑动
