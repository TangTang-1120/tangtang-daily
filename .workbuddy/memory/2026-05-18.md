# 2026-05-18

## GitHub Pages 公网部署完成

- 创建 GitHub 仓库：**TangTang-1120/tangtang-daily**（公开）
- 上传全部 16 个文件：daily-checklist.html、manifest.json、icons/ (11个PNG)、apple-touch-icon.png、favicon、header-bg.png
- 开启 **GitHub Pages**（source-based，main 分支根目录）
- **公网地址**：https://tangtang-1120.github.io/tangtang-daily/daily-checklist.html
- 用户提供了 GitHub PAT token（ghp_KzdtEQO...），通过 API + git push 完成部署
- 注意：token 缺少 workflow scope，无法推送 .github/workflows 文件，改用 source-based Pages 方式

## PWA 安装 404 修复 + manifest 路径修正

- **根因**：manifest.json 中 `start_url` 为绝对路径 `/daily-checklist.html`，PWA 安装后 iOS 访问 `tangtang-1120.github.io/daily-checklist.html`（少了子目录 `/tangtang-daily/`）→ 404
- **修复**：`start_url` 改为 `./daily-checklist.html`（相对路径），图标路径也改为 `./icons/...`
- 新增 `index.html` 自动跳转到 daily-checklist.html，确保根目录可访问
- 用户需要删除旧 PWA 图标后重新安装

## 日历功能改造：去掉收起 + 万年历

### 去掉收起/展开
- 删除 HTML：toggle-btn 按钮、cal-today-bar、cal-today-row 单行视图
- 删除 CSS：.cal-toggle-btn 样式、.cal-body.collapsed 折叠状态、.cal-today-bar/row/day 全套样式
- 删除 JS：`toggleCal()` 函数 + renderCalendar 中的 today-row 填充代码（约30行）
- 日历现在始终完整显示月视图

### 添加万年历数据（节气+节日+假期）
- **24 节气**：SOLAR_TERMS 常量（公历近似日期），日历格子底部绿色小字显示
- **固定节日**：FIXED_FESTIVALS（元旦/情人节/劳动节/国庆等17个）
- **农历节日**：LUNAR_FESTIVALS 查表（2025-2028年，春节/中秋/端午/七夕/重阳/元宵/清明/除夕）
- **法定假期**：PUBLIC_HOLIDAYS 查表（2025-2026年，含调休安排）
- **查询函数**：`getDayInfo(year, month, day)` 返回 { solarTerm, festival, holiday }
- **视觉呈现**：
  - 日历格子：日期数字下方绝对定位小标签（绿色=节气/红色=节日/紫色=休）
  - 详情面板：标题下方彩色 chip 信息条（🌿节气 / 🧧节日 / 🏖️假期）
  - 今日紫色格子的标签自动变为白色半透明

## 去掉3-tab布局，还原到上一步

- 用户要求"去掉tab，还原上一步"
- 使用 `git checkout 25e5457 -- daily-checklist.html` 恢复文件到添加3-tab布局之前的状态
- 提交：`Revert: 去掉3-tab布局，恢复到添加tab前的状态` (76c450a)
- 推送到 GitHub，GitHub Pages 自动部署
- 验证：tab导航已移除，页面恢复到所有内容在同一页面的状态
